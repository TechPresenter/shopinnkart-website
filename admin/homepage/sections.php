<?php
/**
 * ShopInnKart Admin - Sections shortcut.
 *
 * "Sections" is what the storefront calls these rows and "widgets" is what the
 * builder calls them, so both URLs land on the same screen rather than one of
 * them 404-ing on a bookmarked link.
 */

declare(strict_types=1);

require __DIR__ . '/index.php';
