# ShopInnKart - this directory holds server-side code only.
# Nothing in here should ever be requested directly over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
